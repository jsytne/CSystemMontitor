To compile the program according to the project instructions, to build:
```
gcc main.c -o main -pthread
```
To execute with both --logging and --interval of 2 seconds enabled
```
./main --logging --interval 2
```
